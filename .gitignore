node_modules/
.vscode/
dist/
*.log
*.tsbuildinfo
/.next/
/out/
.env*
.vercel
.omc/
.omo/
.letta/

# Local checkout stashes (do not commit)
_stash_*/

# Bulk vendor/source archive for BEAST-01. Keep binaries in object storage, not git.
UGV-Beast-Archive/

# Playwright/UI review scratch (node_modules inside)
/.tmp/

# Local library ingest / CAD LFS worktrees (never commit)
.tmp-*/
.tmp-library-staging/
.tmp-cad-lfs/
.tmp-n5-mirror/

# Vendor source archives — re-downloadable, recorded in keyArtifactstosort/reference/INDEX.md
keyArtifactstosort/reference/*.zip

# machine-local agent settings stay out of git (frozenSkillz project-agent-config convention)
.claude/settings.local.json
