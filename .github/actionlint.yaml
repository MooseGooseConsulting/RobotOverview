# Context7 checked: rhysd/actionlint config.md — self-hosted-runner.labels
# and config-variables for vars.* used by cold-review.yml.
self-hosted-runner:
  labels:
    - moosegoose-linux
config-variables:
  - OPENHANDS_COLD_REVIEW_APP_ID
