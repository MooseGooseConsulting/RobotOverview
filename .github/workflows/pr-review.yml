name: PR Review Trigger

on:
  pull_request:
    types: [labeled]

jobs:
  review:
    if: github.event.label.name == 'review-this'
    uses: MooseGooseConsulting/coldaine-codeOps/.github/workflows/openhands-codex-pr-review.yml@main
    secrets: inherit
