[Unit]
Description=BEAST-01 link watchdog — recover underlay + Tailscale
After=network-online.target
ConditionPathExists=/usr/local/sbin/beast-link-watch

[Service]
Type=oneshot
ExecStart=/bin/sh -c 'exec /usr/local/sbin/beast-link-watch'
